# Set vim mode
set editing-mode vi

# Set autocompletion case insensitive
set completion-ignore-case on

# vi settings
# bind -P for show bindings
$if mode=vi
	# binding in command mode
	set keymap vi-command
	"\C-s": vi-subst

	# binding in insert mode
	set keymap vi-insert
$endif
